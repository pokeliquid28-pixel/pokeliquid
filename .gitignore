# Next.js build files
app/.next/
.next/

# Local environment variables (API keys)
app/.env.local
.env.local
.env

# Dependencies (if not already there)
node_modules/
.anchor
.DS_Store
target
**/*.rs.bk
node_modules
test-ledger
.yarn
.surfpool
